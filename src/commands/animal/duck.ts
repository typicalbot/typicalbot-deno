// import Command from "../../common/command/Command.ts";
// import { bot } from "../../cache.ts";

// const DuckCommand: Command = (message) => {
//   fetch("https://random-d.uk/api/v2/quack")
//     .then((res) => res.json())
//     .then((json) => message.send(json.url))
//     .catch(() => message.send("Unable to query from API."));
// };

// bot.commands.set("duck", DuckCommand);
